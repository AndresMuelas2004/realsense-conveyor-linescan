# Normalize line endings: store LF in the repository for all text files.
* text=auto eol=lf

# Treat data and image artifacts as binary (never apply EOL conversion).
*.npz binary
*.npy binary
*.png binary
*.jpg binary
